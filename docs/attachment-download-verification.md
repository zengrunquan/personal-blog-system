# 10.3 附件原始文件名下载验收记录

更新日期：2026-09-08。实现基于 10.2 提交 `ac4a8f7`；本记录更新时，10.3 的代码、测试和文档尚未提交，由用户自行处理 Git 提交。

## 结论与边界

10.3 已完成原名查询、安全响应头、旧附件兜底和错误处理实现；独立代码审查未发现范围内可确认的新 bug。V-01 已补齐两份附件的实际文件名和内容一致性验收。

源码改动限于下载 Servlet、媒体 DAO 查询、名称服务及下载工具；另有对应测试和说明文档。没有为本修复修改前端业务、附件 URL、上传存储结构、数据库表、媒体引用或清理逻辑。原名只用于下载展示，不参与物理路径解析。历史附件缺少真实原名时仍使用稳定 URL 名，数据库查询故障按约定返回 500。

## 自动化与独立审查

| 验证 | 结果与来源 |
| --- | --- |
| 后端单元测试 | 执行任务及独立审查均为 169 项：168 通过、1 跳过，0 failures、0 errors |
| 文件名补充检查 | 独立审查通过 8 组编码往返及 CR/LF/NUL 控制字符检查 |
| 前端与打包 | 执行任务记录：lint、13 项 Vitest、TypeScript/Vite 构建、WAR 打包通过 |
| 改动边界 | 独立审查核对原实现差异为 9 个修改文件及 2 个新文件，均在计划内 |

独立审查命令（在 `backend` 目录）：

```powershell
.\mvnw.cmd '-Dnet.bytebuddy.experimental=true' '-Dblog.test.media.database=false' test
```

跳过项为显式启用的媒体数据库兼容测试，不是下载原名的实际数据库集成测试。Byte Buddy 参数仅用于本机 JDK 测试兼容，不是业务配置。

## V-01 实际运行证据

证据来自任务“修复附件下载原始文件名”（`01a07ef2-2022-7980-b0de-828cefcb3e3a`）2026-09-08 的运行记录和用户手动下载反馈；本文整理阶段没有重新执行浏览器下载。

| 场景 | 下载名 | 大小 | 内容核对 |
| --- | --- | ---: | --- |
| 管理员下载已有附件 | `opencode.pdf` | 425137 字节 | 执行任务确认与服务器附件 SHA-256 一致 |
| 普通用户下载已有附件 | `WSL.docx` | 30138 字节 | 用户手动下载后，执行任务再次只读核对 SHA-256 一致 |
| 未登录访问下载接口 | 不适用 | 不适用 | 执行任务记录返回 401 |

普通用户样本 SHA-256：

```text
42DF5A8B09FF6E3FA2B4DAD3AF89989AA2CCF0D5106A9D3ACEB3957A65C2AB91
```

执行期间自动化 Edge 的二进制下载被 `ERR_BLOCKED_BY_CLIENT` 拦截，自动化工具没有完成第二份文件落盘；随后由用户在普通 Edge 手动下载，再由执行任务核对文件名、长度和哈希。不能把上述流程写成自动化浏览器全程通过。

本文更新时，原记录中的两个本地下载副本已不在原目标路径，因此没有重新进行两侧哈希比较；服务器对应文件的长度仍与记录相符，DOCX 哈希仍与上述证据相符。此事实不改变之前已记录的验收结果，也不证明现在下载副本仍被保留。

## 覆盖限制

- 已覆盖两份已有附件的原名与内容，以及普通用户与管理员场景。
- 没有取得实际浏览器响应头读取、特殊字符新上传、无扩展名、同名不同内容、兼容 URL 实际落盘、重新部署后下载的完整运行证据；相关编码和兼容行为已有单元测试，不冒充浏览器证据。
- V-01 本次按“实际文件落盘验收”关闭；上述扩展场景保留为后续运行回归建议，不列为已发现代码 bug。
- 本结果不替代媒体生命周期独立 MySQL 8 迁移、回填、清理及多连接并发验收。
